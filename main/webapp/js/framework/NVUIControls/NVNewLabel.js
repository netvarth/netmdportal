function newlabel(button){this.result=function(){var pTag=$j('<p/>');var spanTag=$j('<span/>');var title=button.title;if(button.titleimage)title+='( <img src="'+button.titleimage+'"/>)';spanTag.html(title);if(button.labelId)spanTag.attr('id',button.labelId);var labelTag=$j('<label/>');labelTag.attr('name','lblValue');pTag.append(spanTag);if(button.title){var brTag=$j('<br/>');pTag.append(brTag)}if(button.spanClass)spanTag.attr('class',button.spanClass);if(button.name)pTag.attr('name',button.name);if(button.image)spanTag.append('<img src ="'+button.image+'" /> ');if(button.id)pTag.attr('id',button.id);if(button.className)pTag.attr('class',button.className);if(button.style)pTag.attr('style',button.style);if(button.value)labelTag.html(button.value);pTag.append(labelTag);return pTag}}